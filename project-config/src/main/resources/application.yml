spring:
  application:
    name: project-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhouxiaoyang/testConfig.git
          username:
          password:
          #这个地址是config server 从git仓库中拉取的配置文件的本地存放位置
          basedir: E:\gitforspringcloud\springCloudDemo\project-config\basedir
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8001/eureka/